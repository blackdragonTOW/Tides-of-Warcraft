namespace = bookmarksetup

#This is to prevent stuff like holy light foundation and other such events from firing in startdates where they already happened.

#Also sets modifiers and flags to specific provinces.

#Sets the flags
character_event = {
	id = bookmarksetup.1
	hide_window = yes
	
	is_triggered_only = yes
	
	immediate = {
		#Specific variables
		##DOOM
		set_variable = { which = global_doom value = 0 }
		#Deal with titles
		#Add province modifiers
		#Kill specific characters (usually generic scourge ones that don't have any business being around before their time)
		##Also for eliminating characters that leave the map (such as the Darkspears)
		if = {
			limit = {
				NOT = { year = 10003 }
			}
			c_29 = { # Mai'Zoth Mosh'Ogg
				death = { death_reason = death_missing }
			}
		}
		if = {
			limit = {
				year = 10015
			}
			c_23 = { # Sen'jin Darkspear
				death = { death_reason = death_missing }
			}
			c_24 = { # Vol'jin Darkspear
				death = { death_reason = death_missing }
			}
			c_26 = { # Gadrin Eniku
				death = { death_reason = death_missing }
			}
			c_27 = { # Zalazane Yyha
				death = { death_reason = death_missing }
			}
		}
		if = {
			limit = {
				NOT = { year = 10021 }
			}
			c_11 = { # Garr Salthoof
				death = { death_reason = death_missing }
			}
			c_12 = { # Severance Wavetail
				death = { death_reason = death_missing }
			}
			c_13 = { # Abrogaz Seahorn
				death = { death_reason = death_missing }
			}
		}
		if = {
			limit = {
				NOT = { year = 10023 }
			}
			c_22 = { # Sakrasis Thirass
				death = { death_reason = death_missing }
			}
		}
		#Destroy settlements based on the date
		if = {
			limit = {
				always = no
			}
			b_sunstone = {
				destroy_settlement = THIS
			}
		}
		#Add buildings ## Due to us going past year 10k, buildings added through history won't appear (unless it was fixed)
	}
}