namespace = miscevt

#For stuff that won't fit anywhere else

#Battle duel
character_event = {
	id = miscevt.1
	hide_window = yes
	
	is_triggered_only = yes
	
	immediate = {
		random_character = {
			limit = {
				war_with = ROOT
				at_location = ROOT
				in_battle = yes
				in_command = yes
			}
			ROOT = {
				set_character_flag = duel_cannot_surrender
			}
			set_character_flag = duel_cannot_surrender
			character_event = { id = duel.1 }
		}
	}
}